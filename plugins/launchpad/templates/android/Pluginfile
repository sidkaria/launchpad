# launchpad Android — generated. Re-run /launchpad:setup to regenerate. Do not hand-edit.
# Declares the fastlane plugins the `beta` lane uses. The CI workflow installs
# the matching gem; fastlane loads it from here. Committed on purpose so no
# interactive `fastlane add_plugin` is ever needed.
gem 'fastlane-plugin-firebase_app_distribution'
