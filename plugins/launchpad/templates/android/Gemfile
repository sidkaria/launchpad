# launchpad Android — generated. Re-run /launchpad:setup to regenerate. Do not hand-edit.
# fastlane only auto-loads plugins through bundler: the Gemfile must eval the
# Pluginfile, and the lane must run under `bundle exec`. Installing the plugin
# gem alone is NOT enough — fastlane then reports
# "Could not find action, lane or variable 'firebase_app_distribution'".
source "https://rubygems.org"

gem "fastlane"

plugins_path = File.join(File.dirname(__FILE__), "fastlane", "Pluginfile")
eval_gemfile(plugins_path) if File.exist?(plugins_path)
